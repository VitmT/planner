{% extends "base.html.twig" %}

{% block logout %}
        <form class="logout" action="{{ path('logout') }}" method="post">
                    <button class="btn btn-outline-danger position-absolute top-0 end-0 mt-2 me-2" ><i class="bi bi-box-arrow-right"></i> &nbsp;Logout</button>
        </form>
{% endblock %}

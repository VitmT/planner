{% extends "adminbase.html.twig" %}
{% block body %}
<div class="container mt-5">
<a class="btn btn-light back-button" href="{{path('app_index')}}" role="button">Zpět</a>
    <div class="row d-flex justify-content-center">
        <div class="col-md-6">
            <div class="card px-5 py-5">
                <div class="form-data">
                    <div class="d-grid gap-2">
                      <div class="list-group text-center">
                        {% for reccuringEventOccurence in reccuringEventOccurences %}
                            <a href="#" class="list-group-item list-group-item-action">{{reccuringEventOccurence.place}}</a>
                        {% endfor %}
                      </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

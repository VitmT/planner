{% extends "adminbase.html.twig" %}
{% block body %}
<div class="container mt-5">
<a class="btn btn-outline-dark position-absolute top-0 start-0 mt-2 ms-2" href="{{path('event-list')}}" role="button"><i class="bi bi-arrow-return-left"></i> &nbsp; Back</a>
    <div class="row d-flex justify-content-center">
        <div class="col-md-6">
            <div class="card px-5 py-5">
                <div class="form-data">
                    <div class="d-grid gap-2 justify-content-center">
                        <h2>Create new event</h2>   
                        {{ form(form) }}

                        {% if showDelete %}
                        <a href="{{ path('delete-event', {'occurence': occurence.id})}}" class="btn btn-outline-danger">
                        <i class="bi bi-trash"></i> Delete
                        {% else %}
                        <p>&nbsp;</p>
                        {% endif %}
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    </div>
{% endblock %}
